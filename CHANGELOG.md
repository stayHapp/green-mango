# CHANGELOG

## Unreleased

- 支持无手机号嘉宾：Excel 导入时手机号留空自动填充默认手机号 `13900010001`，导出显示「未提供」，默认号码不公开在登录页、由会务告知。
- 管理后台嘉宾列表支持多选批量软停用，报名申请行不可选，停用后历史签到保留。
- Excel 导入同会议同名同手机号嘉宾改为覆盖更新（保留签到记录），导入结果展示新增、更新、默认手机号与错误行统计。
- Excel 导入放宽表头要求：只需包含姓名列，手机号列可省略或留空（自动用默认号），表头顺序任意、多余列自动忽略。
- 嘉宾端会话失效（如嘉宾被停用）时错误页提供「退出登录」入口，避免被困在权限错误页。
- 嘉宾身份（标签）为空时默认显示「参会嘉宾」，覆盖嘉宾端、工作人员端与登录会话的展示兜底。
- 工作人员端模块职责梳理：手动签到仅展示未签到嘉宾，搜索与同行登记相互独立；同行登记仅支持已签到主嘉宾且可展开查看同行明细；签到记录新增搜索并按已签到、未签到分组展示，统计区分主嘉宾与同行口径，四个统计数字可点击过滤；同行人员统计按当前场次口径计算，保证数字与明细一致。
- 会议服务「仅嘉宾可见提示」支持后台按会议自定义：默认文案调整为「此项服务仅对已登录参会人员开放」，未登录嘉宾点击受限服务时展示后台配置的提示。
- 工作人员端同行登记列表排除同行嘉宾：同行人员不再显示「添加同行」入口，避免误操作触发后端拒绝。
- 后端启动改为 2 个进程（Docker 镜像默认命令与 systemd 服务同步更新），充分利用 2 核 CPU；Nginx 静态资源增加永久缓存头、首页保持不缓存，显著降低 3M 带宽压力。
- 嘉宾端会议日程页日期切换器改为完整显示「8月18日」样式，月份与日期合并展示，星期单独一行。
- 域名根路径改为默认进入指定会议的公开首页：会议 ID 由前端构建环境变量 `VITE_PUBLIC_DEFAULT_MEETING_ID` 配置，未配置时回退到管理员登录页；「后台配置默认入口会议」记录为后续版本优化方向。
- 新增并定制新服务器与新域名部署文档，覆盖 `49.233.60.2`、`meeting.cssat.cn`、TencentOS、PostgreSQL 16.6、`/opt/green-mango`、旧数据迁移流程和已有 Web 服务共存排查。
- 管理员端会议入口二维码本地缓存复用：域名未变动时刷新直接显示之前二维码；下载图片标题自动换行完整居中。
- 会议二维码下载图片标题按括号位置断行；下载时间显示与会议设置的「上午/下午 / 具体时间」模式一致。
- 归档工作人员端同行人员登记执行计划，并同步项目入口文档到 MVP 联调验收阶段。
- 新增工作人员端同行人员登记：工作人员可为主嘉宾登记同行人员，同行嘉宾自动关联主嘉宾并写入当前场次签到。
- 工作人员端签到记录展示同行绑定：主嘉宾行显示「已带 N 人」徽标并可展开查看同行人员，同行记录行显示所属嘉宾与随行备注。
- 签到明细和嘉宾状态导出新增「嘉宾类型」「陪同嘉宾」信息，嘉宾来源支持「同行登记」。
- 新增多签到场次管理、当前有效场次、按场次统计、相邻场次差异和按场次导出能力。
- 管理员端日程编辑器支持整段文本批量解析生成日程（Tab / 竖线分隔、跨行内容、占位符自动识别，可预览后覆盖或追加），环节改为紧凑列表并支持内部滚动。
- 管理员端日程支持环节与时段拖拽排序（可跨时段移动），时段内相邻环节时间乱序或重叠时行内显示中文提示。
- 新增会议资料独立管理，支持多条资料、受限正文编辑、附件上传与下载权限校验。
- 新增会议助手公开/仅登录嘉宾可见访问级别，并完善未发布内容防泄露规则。
- 新增联系会务二维码、路线导航地点确认和天气服务生产集成能力。
- 完成管理员端、嘉宾端、工作人员端主要 API 与前端联调，包括会议、嘉宾、工作人员、签到、会议服务和资料模块。
- 将本地联调和正式环境数据库方向调整为 PostgreSQL，SQLite 保留为本地临时回退方案。
- 调整产品文档方向，明确管理员端、嘉宾端、工作人员端及扫码签到闭环。
- 修正后端既有代码注释和 docstring 为简体中文，并更新执行计划模板的注释规范。
- 新增第一版核心 SQLAlchemy ORM 模型。
- 新增首个 Alembic 业务迁移，创建 6 张核心表。
- 新增核心模型 metadata 和约束测试。
- 更新数据库和后端架构文档，记录模型与迁移状态。
- 将后端运行环境调整为 Python 3.12。
- 初始化 SQLAlchemy 数据库基础设施。
- 初始化 Alembic 迁移骨架。
- 引入 `pydantic-settings` 管理后端配置。
- 新增数据库基础设施测试。
- 初始化后端 FastAPI 最小骨架。
- 新增 `GET /api/health` 健康检查接口和 pytest 测试。
- 更新项目检查脚本，纳入后端测试。
- 添加 `.gitignore`，忽略环境文件、虚拟环境和缓存文件。
- 初始化项目骨架。
- 建立产品、架构、技术决策和执行计划文档目录。
- 添加占位检查脚本。
