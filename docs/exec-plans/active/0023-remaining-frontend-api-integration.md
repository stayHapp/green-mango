# 剩余前后端 API 联调

## 背景

登录会话、管理员会议和管理员嘉宾基础资源已经完成真实 API 联调。管理员 Excel、工作人员资源、签到统计、工作人员工作台和嘉宾业务页仍有 Mock 调用。

## 目标与顺序

1. 管理员 Excel 模板下载、嘉宾导入和签到表导出。
2. 管理员工作人员维护。
3. 管理员签到统计与明细。
4. 工作人员负责会议、嘉宾搜索、扫码与手动签到。
5. 嘉宾会议、个人资料和签到二维码。

每次只完成并验证其中一个明确步骤，完成后在本文记录结果，再进入下一项。

## 当前任务

联调第 1 项管理员 Excel 接口，删除详情页对浏览器本地 XLSX 解析、生成和 Mock 导入的依赖。

## 验收标准

- 模板下载调用后端会议模板接口。
- `.xlsx` 名单以 multipart/form-data 上传后端，并展示成功数和逐行错误。
- 签到表直接下载后端生成的全量签到 XLSX。
- 前端生产构建和后端完整测试通过。

## 验证方式

- `cd frontend && npm run build`
- `cd backend && .venv/bin/pytest`
- 启动前后端后使用本地种子数据进行浏览器联调。
- `git diff --check`

## 联调记录

### 第 1 项：管理员 Excel

- 已完成模板下载、XLSX 名单上传和签到明细下载的真实 API 接入。
- 已删除页面端 XLSX 解析、生成和 Mock 导入逻辑，并移除前端 `xlsx` 依赖。
- 浏览器使用管理员种子账号进入会议 ID `1`，确认导入页签、模板下载和签到表导出入口可操作。
- 前端生产构建通过；后端 23 项测试通过；`git diff --check` 通过。
- 浏览器控制接口不支持为隐藏文件输入框注入本地文件，上传结果由后端真实工作簿往返自动化测试覆盖；后续人工选择文件时页面直接走同一上传接口。
